import { CazzeonFormComponent } from './cazzeon-form-component';

describe('CazzeonFormComponent', () => {
  it('should create an instance', () => {
    expect(new CazzeonFormComponent()).toBeTruthy();
  });
});
