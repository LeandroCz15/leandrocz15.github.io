import { IconSrcResolverPipe } from './icon-src-resolver.pipe';

describe('IconSrcResolverPipe', () => {
  it('create an instance', () => {
    const pipe = new IconSrcResolverPipe();
    expect(pipe).toBeTruthy();
  });
});
