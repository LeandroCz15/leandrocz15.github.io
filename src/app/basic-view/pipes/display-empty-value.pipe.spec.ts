import { DisplayEmptyValuePipe } from './display-empty-value.pipe';

describe('DisplayEmptyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayEmptyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
