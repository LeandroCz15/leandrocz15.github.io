import { MultiCaseSwitchPipe } from './multi-case-switch.pipe';

describe('MultiCaseSwitchPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiCaseSwitchPipe();
    expect(pipe).toBeTruthy();
  });
});
