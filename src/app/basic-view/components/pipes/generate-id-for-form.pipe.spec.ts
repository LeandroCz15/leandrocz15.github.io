import { GenerateIdForFormPipe } from './generate-id-for-form.pipe';

describe('GenerateIdForFormPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateIdForFormPipe();
    expect(pipe).toBeTruthy();
  });
});
